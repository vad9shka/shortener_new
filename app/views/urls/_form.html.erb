<%= form_for(@url) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url_long %><br>
    <%= f.text_field :url_long %>
  </div>
  <div class="field">
    <%= f.label :url_short %><br>
    <%= f.text_field :url_short %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
